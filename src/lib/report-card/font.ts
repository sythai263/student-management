/**
 * Serif stack applied to the whole report card — Times New Roman per the
 * standard form ("thể thức văn bản"). Inline style because Tailwind has no
 * utility for this exact stack.
 */
export const REPORT_CARD_FONT_FAMILY = '"Times New Roman", Times, serif';
